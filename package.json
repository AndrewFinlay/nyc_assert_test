{
  "name": "nycassert",
  "version": "1.0.0",
  "license": "ISC",
  "main": "lib/assert.js",
  "scripts": {
    "test": "npx mocha --recursive --ui bdd ./test",
    "cover": "npx nyc --no-cache --clean mocha --recursive --ui bdd ./test",
    "cover_no_instrument": "node node_modules/nyc/bin/nyc --no-cache --no-instrument --clean mocha --recursive --ui bdd ./test"
  },
  "dependencies": {
    "mocha": "^6.1.4",
    "nyc": "^15.0.0-beta.1"
  }
}
